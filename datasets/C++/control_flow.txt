#include <iostream>
int main() {
    int number = 10;
    if (number > 0) {
        std::cout << "Positive" << std::endl;
    } else {
        std::cout << "Non-positive" << std::endl;
    }
    return 0;
}
// Uses an if-else statement to check if a number is positive.